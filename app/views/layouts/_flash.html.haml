- flash.each do |name, msg|
  - next unless msg.is_a?(String)

  .fixed.top-4.right-4.z-50
    .rounded-lg.shadow-lg.px-6.py-4{ class: "#{name.to_s == 'notice' ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800'}" }
      = msg
